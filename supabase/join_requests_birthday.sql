-- ==========================================================
-- ДР в заявке на регистрацию (join_requests.birthday)
-- Выполнить в Supabase SQL Editor
-- ==========================================================

-- Необязательное поле: человек может указать день рождения прямо на
-- экране регистрации. При одобрении заявки значение переносится в
-- users.birthday (см. lib/telegramApprovals.js).
alter table public.join_requests add column birthday date;
