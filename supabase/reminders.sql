-- ==========================================================
-- Персональные напоминания "не забудь отправить заявку" —
-- каждый сотрудник сам выбирает время и вкл/выкл в Настройках.
-- Рассылает sales-bot (крутится постоянно на Railway, проверяет
-- раз в минуту) — не через Vercel cron, там нет нужной частоты
-- на бесплатном плане.
-- Выполнено напрямую через подключение к БД.
-- ==========================================================

alter table public.users
  add column reminder_enabled boolean not null default false,
  add column reminder_time time,
  add column reminder_last_sent_date date;
